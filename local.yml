---
- hosts: localhost
  connection: local
  tasks:
  - name: install gems
    gem: name={{ item }} state=latest executable=/usr/bin/gem
    with_items:
    - keen
    - dotenv
  - name: run heartbeat
    shell: /gorilla/heartbeat
